class_name Character extends Item
# TODO: Double check class declarations, Character objects should behave the same as Items

# Called when the node enters the scene tree for the first time.
func _ready():
	super()
# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	super(delta)
